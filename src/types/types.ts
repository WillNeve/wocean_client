export type staticComponentProps = {
  children?: React.ReactNode,
  className?: string,
};




// Note

export type note = {
  id: number,
  title?: string,
  body?: string,
  order: number,
  folder: boolean,
}
