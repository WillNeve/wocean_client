export type staticComponentProps = {
  children?: React.ReactNode,
  className?: string,
};




// Note

export type note = {
  id: number,
  title?: string,
  body?: string,
  note_order: number,
  folder: boolean,
  folder_id: number,
}
